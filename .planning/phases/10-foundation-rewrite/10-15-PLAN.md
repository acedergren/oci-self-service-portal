---
phase: 10-foundation-rewrite
plan: 15
type: execute
wave: 12
depends_on:
  - 10-14
files_modified:
  - apps/api/src/mastra/rag/oracle-vector-store.ts
  - packages/server/src/oracle/vector-utils.ts
  - packages/server/src/oracle/index.ts
  - packages/server/src/oracle/__tests__/vector-utils.test.ts
  - apps/api/src/tests/rag/oracle-vector-store.test.ts
autonomous: true
must_haves:
  truths:
    - 'Vector store binds Float32Array values directly via DB_TYPE_VECTOR.'
    - 'Legacy string conversion helpers are removed.'
    - 'Tests verify vectors are searchable immediately after insert and VPD isolation is enforced.'
  artifacts:
    - path: 'apps/api/src/mastra/rag/oracle-vector-store.ts'
      provides: 'Mastra vector store implementation using typed bindings'
    - path: 'apps/api/src/tests/rag/oracle-vector-store.test.ts'
      provides: 'Regression tests for vector search behavior'
  key_links:
    - from: 'apps/api/src/mastra/rag/oracle-vector-store.ts'
      to: 'packages/server/src/oracle/vector-utils.ts'
      via: 'import'
      pattern: "from '@portal/server/oracle/vector-utils'"
    - from: 'apps/api/src/tests/rag/oracle-vector-store.test.ts'
      to: 'DB_TYPE_VECTOR'
      via: 'expect'
      pattern: 'DB_TYPE_VECTOR'
---

<objective>
Integrate the Oracle 26AI enhancements into the Mastra vector store by using typed bindings, removing legacy conversion helpers, and validating functionality with tests.

Purpose: Achieve PRD vector performance and correctness goals following the schema migration.
Output: Updated vector store code, utilities, and regression tests.
</objective>

<execution_context>
@/Users/acedergr/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/acedergr/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/reference/PRD.md
@apps/api/src/mastra/rag/oracle-vector-store.ts
</context>

<tasks>

<task type="auto">
  <name>Refactor vector store to use typed bindings</name>
  <files>apps/api/src/mastra/rag/oracle-vector-store.ts</files>
  <action>
  - Replace string-based vector serialization with direct Float32Array binding using updated helpers.
  - Adjust query logic to leverage HNSW indexes and ensure tenant filtering uses VPD policies.
  </action>
  <verify>pnpm --filter @portal/api build</verify>
  <done>Vector store binds Float32Array values directly and compiles.</done>
</task>

<task type="auto">
  <name>Remove legacy vector utilities</name>
  <files>packages/server/src/oracle/vector-utils.ts</files>
  <action>
  - Delete `vectorToOracleString` and related helpers; replace with typed binding utilities.
  - Update unit tests in `packages/server/src/oracle/__tests__/vector-utils.test.ts` to validate new helpers.
  </action>
  <verify>pnpm --filter @portal/server build</verify>
  <done>Legacy string helpers removed with updated unit tests covering typed bindings.</done>
</task>

<task type="auto">
  <name>Add regression tests for vector search</name>
  <files>apps/api/src/tests/rag/oracle-vector-store.test.ts</files>
  <action>
  - Ensure tests insert embeddings and assert search returns results immediately (mocking Oracle responses where necessary).
  - Validate VPD isolation by simulating multiple org IDs.
  </action>
  <verify>npx vitest run apps/api --run tests/rag/oracle-vector-store.test.ts</verify>
  <done>Tests confirm immediate searchability and tenant isolation with typed vector bindings.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @portal/api build`
- `pnpm --filter @portal/server build`
- `npx vitest run apps/api --run tests/rag/oracle-vector-store.test.ts`
</verification>

<success_criteria>

- Vector store uses typed bindings with passing regression tests proving performance and isolation expectations.
  </success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-rewrite/10-15-SUMMARY.md`
</output>
