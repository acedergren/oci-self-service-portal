/**
 * Re-export from @portal/types for internal relative import compatibility.
 * Files in this package use `import { ... } from '../errors'` / `'./errors'`.
 */
export * from '@portal/types/errors.js';
