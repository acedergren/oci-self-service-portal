/**
 * @portal/types - Shared TypeScript types and Zod schemas
 *
 * This package will contain all shared type definitions, Zod schemas,
 * and validation utilities used across frontend and backend.
 */

// Placeholder export - will be populated with actual types in subsequent tasks
export const PACKAGE_VERSION = '0.0.1';
